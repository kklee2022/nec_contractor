"""Compensation Events app configuration."""

from django.apps import AppConfig


class CompensationEventsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.compensation_events'
    label = 'compensation_events'
